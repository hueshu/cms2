{
  "name": "cf-cms-site",
  "version": "1.0.0",
  "description": "Multi-site CMS frontend powered by Cloudflare Pages",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "keywords": ["cloudflare", "pages", "cms", "multi-site"],
  "author": "HueShu",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "marked": "^11.0.0"
  }
}